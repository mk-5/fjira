version: "2"

run:
  timeout: 4m
  issues-exit-code: 1
  tests: true
  modules-download-mode: mod
  allow-parallel-runners: true
  go: "1.25"

linters:
  exclusions:
    paths:
      - out

formatters:
  exclusions:
    paths:
      - out
